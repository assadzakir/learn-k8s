sudo: required
services:
  - docker

before_install:
  - docker build -t assadzakir/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run assadzakir/react-test npm test -- --coverage

after_success:
  - docker build -t assadzakir/multi-client ./client
  - docker build -t assadzakir/multi-nginx ./nginx
  - docker build -t assadzakir/multi-server ./server
  - docker build -t assadzakir/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push assadzakir/multi-client
  - docker push assadzakir/multi-nginx
  - docker push assadzakir/multi-server
  - docker push assadzakir/multi-worker

